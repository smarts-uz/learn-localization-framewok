import { HeadMetadata } from "@plasmicapp/query";
import { ClientReferenceVisitor } from "@plasmicapp/react-ssr-prepass";
import React from "react";
interface PlasmicPrepassData {
    queryData: Record<string, any>;
    headMetadata: HeadMetadata;
}
/**
 * Runs prepass on the React tree and returns all data for prerendering a Plasmic page.
 */
export declare function plasmicPrepassExtract(element: React.ReactElement, onClientComponentRef?: ClientReferenceVisitor): Promise<PlasmicPrepassData>;
/**
 * Runs prepass on the React tree and returns query data for prerendering a Plasmic page.
 */
export declare function extractPlasmicQueryData(element: React.ReactElement, onClientComponentRef?: ClientReferenceVisitor): Promise<Record<string, any>>;
/**
 * Runs prepass on the React tree.
 */
export declare function plasmicPrepass(element: React.ReactElement, onClientComponentRef?: ClientReferenceVisitor): Promise<void>;
export {};
